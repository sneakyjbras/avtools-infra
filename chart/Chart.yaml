apiVersion: v2
name: avtools
description: >-
  AV Tools sharded SNMP/ping sweep on CERN Kubernetes (Magnum) — three batch
  services (snmp-timeseries, run-eam, run-landb) as Indexed CronJobs, plus an
  optional Fluent Bit log shipper to CERN MonIT OpenSearch.
type: application
version: 0.1.0
appVersion: "1.8.7"
maintainers:
  - name: avtools-admins
    email: avtools-admins@cern.ch
